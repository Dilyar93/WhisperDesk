faster-whisper==1.0.3
ctranslate2==4.4.0
# ctranslate2 4.4 wheel 不带任何 CUDA runtime DLL，必须显式拉 NVIDIA 的 pip 包并打进 _internal。
# 下面这套是 CUDA 12 + cuDNN 8 的完整 runtime 闭包：
#   - cuda-runtime-cu12 → cudart64_12.dll
#   - cublas-cu12       → cublas64_12.dll, cublasLt64_12.dll
#   - cudnn-cu12 (8.x)  → cudnn_*_8.dll 一组
# 注意 cuDNN 必须是 8.x（ctranslate2 4.4 找 *_8.dll 的硬编码名字），9.x 不兼容。
nvidia-cuda-runtime-cu12>=12.1,<13 ; platform_system == "Windows"
nvidia-cublas-cu12>=12.1,<13 ; platform_system == "Windows"
nvidia-cudnn-cu12==8.9.7.29 ; platform_system == "Windows"
# 必须钉 numpy<2：numpy 2.x 的 _core._multiarray_umath 用 pybind11 单阶段初始化，
# 若被 PyInstaller 重复打包会抛 `cannot load module more than once per process`。
numpy>=1.24,<2
# faster-whisper 1.0.3 依赖（本地 import 探测验证的完整运行时闭包）
av>=11,<13                         # 音频解码（PyAV），带 FFmpeg C 扩展
# onnxruntime 必须钉在 1.18.x：1.19+ 在 Windows 上与 ctranslate2 的 CUDA/OpenMP
# 共存时会出 `DllMain 初始化失败`，这是 onnxruntime 社区 issue #21764 / #22007
# 反复确认的兼容窗口。faster-whisper 1.0.3 的 silero VAD 不需要新特性。
onnxruntime==1.18.1               # VAD（Silero）必需，带原生 DLL
tokenizers>=0.15,<0.20
# huggingface_hub 1.x 换了 httpx 且 API 破坏，必须锁在 0.x 代
huggingface_hub>=0.23,<1
# HTTP/下载链路（faster_whisper.utils 直接 import requests）
requests>=2.31.0
certifi>=2024.2.2
urllib3>=2.0.0
charset_normalizer>=3.3.0
idna>=3.6
# hub 链路常见
filelock>=3.13
fsspec>=2024.3.0
tqdm>=4.66
pyyaml>=6.0
packaging>=23.0
# GUI
PySide6==6.7.2
pyqtdarktheme==2.1.0
